// import Router from "express";
// import passport from "passport";

// import UserController from "../controllers/user.controllers";
// const router = Router();

// router.post("/signup", UserController.signup);
// router.post("/login", passport.authenticate("local", { failureRedirect: "/api/v1/login" }), UserController.login);
// //router.put("/update_profile", passport.authenticate("jwt", { session: false }), UserController.updateUserProfile);


// export default router;
